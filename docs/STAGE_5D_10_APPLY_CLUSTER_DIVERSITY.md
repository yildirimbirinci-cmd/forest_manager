# Stage 5D.10 - Apply Cluster Diversity Mode

Changes only the Forest Pack Diversity mode from the current value to Clusters (`divers = 2`).

Runtime verification protects the existing cluster parameters, density, geometry probabilities, native scale variation, rotation, and translation state. If verification fails, the previous diversity mode is restored.

Bridge version: 0.9.24
